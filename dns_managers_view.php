<?php
// This script and data application were generated by AppGini 23.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/dns_managers.php');
	include_once(__DIR__ . '/dns_managers_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('dns_managers');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'dns_managers';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`dns_managers`.`id`" => "id",
		"`dns_managers`.`dns_manager`" => "dns_manager",
		"`dns_managers`.`comments`" => "comments",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`dns_managers`.`id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`dns_managers`.`id`" => "id",
		"`dns_managers`.`dns_manager`" => "dns_manager",
		"`dns_managers`.`comments`" => "comments",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`dns_managers`.`id`" => "ID",
		"`dns_managers`.`dns_manager`" => "Provider",
		"`dns_managers`.`comments`" => "Comments",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`dns_managers`.`id`" => "id",
		"`dns_managers`.`dns_manager`" => "dns_manager",
		"`dns_managers`.`comments`" => "comments",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`dns_managers` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 0;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'dns_managers_view.php';
	$x->RedirectAfterInsert = 'dns_managers_view.php?SelectedID=#ID#';
	$x->TableTitle = 'DNS managers';
	$x->TableIcon = 'resources/table_icons/dns_setting.png';
	$x->PrimaryKey = '`dns_managers`.`id`';
	$x->DefaultSortField = '2';
	$x->DefaultSortDirection = 'asc';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Provider', ];
	$x->ColFieldName = ['dns_manager', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/dns_managers_templateTV.html';
	$x->SelectedTemplate = 'templates/dns_managers_templateTVS.html';
	$x->TemplateDV = 'templates/dns_managers_templateDV.html';
	$x->TemplateDVP = 'templates/dns_managers_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "col-lg-3";
	$x->DVClasses = "col-lg-9";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: dns_managers_init
	$render = true;
	if(function_exists('dns_managers_init')) {
		$args = [];
		$render = dns_managers_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: dns_managers_header
	$headerCode = '';
	if(function_exists('dns_managers_header')) {
		$args = [];
		$headerCode = dns_managers_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: dns_managers_footer
	$footerCode = '';
	if(function_exists('dns_managers_footer')) {
		$args = [];
		$footerCode = dns_managers_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
