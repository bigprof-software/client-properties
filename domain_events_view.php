<?php
// This script and data application were generated by AppGini 23.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/domain_events.php');
	include_once(__DIR__ . '/domain_events_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('domain_events');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'domain_events';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`domain_events`.`id`" => "id",
		"if(`domain_events`.`datetime`,date_format(`domain_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`domains1`.`domain_name`), CONCAT_WS('',   `domains1`.`domain_name`), '') /* Domain */" => "domain",
		"`domain_events`.`event`" => "event",
		"`domain_events`.`details`" => "details",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`domain_events`.`id`',
		2 => '`domain_events`.`datetime`',
		3 => '`domains1`.`domain_name`',
		4 => 4,
		5 => 5,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`domain_events`.`id`" => "id",
		"if(`domain_events`.`datetime`,date_format(`domain_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`domains1`.`domain_name`), CONCAT_WS('',   `domains1`.`domain_name`), '') /* Domain */" => "domain",
		"`domain_events`.`event`" => "event",
		"`domain_events`.`details`" => "details",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`domain_events`.`id`" => "ID",
		"`domain_events`.`datetime`" => "Datetime",
		"IF(    CHAR_LENGTH(`domains1`.`domain_name`), CONCAT_WS('',   `domains1`.`domain_name`), '') /* Domain */" => "Domain",
		"`domain_events`.`event`" => "Event",
		"`domain_events`.`details`" => "Details",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`domain_events`.`id`" => "id",
		"if(`domain_events`.`datetime`,date_format(`domain_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`domains1`.`domain_name`), CONCAT_WS('',   `domains1`.`domain_name`), '') /* Domain */" => "domain",
		"`domain_events`.`event`" => "event",
		"`domain_events`.`details`" => "details",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['domain' => 'Domain', ];

	$x->QueryFrom = "`domain_events` LEFT JOIN `domains` as domains1 ON `domains1`.`domain_name`=`domain_events`.`domain` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'domain_events_view.php';
	$x->RedirectAfterInsert = 'domain_events_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Domain events';
	$x->TableIcon = 'resources/table_icons/file_extension_log.png';
	$x->PrimaryKey = '`domain_events`.`id`';
	$x->DefaultSortField = '`domain_events`.`datetime`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, ];
	$x->ColCaption = ['Datetime', 'Domain', 'Event', 'Details', ];
	$x->ColFieldName = ['datetime', 'domain', 'event', 'details', ];
	$x->ColNumber  = [2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/domain_events_templateTV.html';
	$x->SelectedTemplate = 'templates/domain_events_templateTVS.html';
	$x->TemplateDV = 'templates/domain_events_templateDV.html';
	$x->TemplateDVP = 'templates/domain_events_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: domain_events_init
	$render = true;
	if(function_exists('domain_events_init')) {
		$args = [];
		$render = domain_events_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: domain_events_header
	$headerCode = '';
	if(function_exists('domain_events_header')) {
		$args = [];
		$headerCode = domain_events_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: domain_events_footer
	$footerCode = '';
	if(function_exists('domain_events_footer')) {
		$args = [];
		$footerCode = domain_events_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
