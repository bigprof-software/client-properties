<?php
// This script and data application were generated by AppGini 23.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/server_events.php');
	include_once(__DIR__ . '/server_events_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('server_events');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'server_events';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`server_events`.`id`" => "id",
		"if(`server_events`.`datetime`,date_format(`server_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`servers1`.`hostname`), CONCAT_WS('',   `servers1`.`hostname`), '') /* Server */" => "server",
		"`server_events`.`event`" => "event",
		"`server_events`.`details`" => "details",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`server_events`.`id`',
		2 => '`server_events`.`datetime`',
		3 => '`servers1`.`hostname`',
		4 => 4,
		5 => 5,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`server_events`.`id`" => "id",
		"if(`server_events`.`datetime`,date_format(`server_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`servers1`.`hostname`), CONCAT_WS('',   `servers1`.`hostname`), '') /* Server */" => "server",
		"`server_events`.`event`" => "event",
		"`server_events`.`details`" => "details",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`server_events`.`id`" => "ID",
		"`server_events`.`datetime`" => "Datetime",
		"IF(    CHAR_LENGTH(`servers1`.`hostname`), CONCAT_WS('',   `servers1`.`hostname`), '') /* Server */" => "Server",
		"`server_events`.`event`" => "Event",
		"`server_events`.`details`" => "Details",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`server_events`.`id`" => "id",
		"if(`server_events`.`datetime`,date_format(`server_events`.`datetime`,'%d/%m/%Y %h:%i %p'),'')" => "datetime",
		"IF(    CHAR_LENGTH(`servers1`.`hostname`), CONCAT_WS('',   `servers1`.`hostname`), '') /* Server */" => "server",
		"`server_events`.`event`" => "event",
		"`server_events`.`details`" => "details",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['server' => 'Server', ];

	$x->QueryFrom = "`server_events` LEFT JOIN `servers` as servers1 ON `servers1`.`id`=`server_events`.`server` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'server_events_view.php';
	$x->RedirectAfterInsert = 'server_events_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Server events';
	$x->TableIcon = 'resources/table_icons/file_extension_log.png';
	$x->PrimaryKey = '`server_events`.`id`';
	$x->DefaultSortField = '`server_events`.`datetime`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, ];
	$x->ColCaption = ['Datetime', 'Server', 'Event', 'Details', ];
	$x->ColFieldName = ['datetime', 'server', 'event', 'details', ];
	$x->ColNumber  = [2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/server_events_templateTV.html';
	$x->SelectedTemplate = 'templates/server_events_templateTVS.html';
	$x->TemplateDV = 'templates/server_events_templateDV.html';
	$x->TemplateDVP = 'templates/server_events_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: server_events_init
	$render = true;
	if(function_exists('server_events_init')) {
		$args = [];
		$render = server_events_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: server_events_header
	$headerCode = '';
	if(function_exists('server_events_header')) {
		$args = [];
		$headerCode = server_events_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: server_events_footer
	$footerCode = '';
	if(function_exists('server_events_footer')) {
		$args = [];
		$footerCode = server_events_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
